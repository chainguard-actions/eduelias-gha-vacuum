openapi: "3.0.0"
info:
  title: Test API
  version: "1.0.0"
  description: A minimal OpenAPI spec for testing vacuum linting
  contact:
    name: Test
    email: test@example.com
  license:
    name: MIT
paths:
  /health:
    get:
      summary: Health check endpoint
      description: Returns the health status of the service
      operationId: getHealth
      responses:
        "200":
          description: Service is healthy
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
                    description: Health status
                    example: ok
